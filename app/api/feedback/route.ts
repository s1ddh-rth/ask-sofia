import { NextResponse } from "next/server";

// Placeholder. The real handler lands in a later phase.
export async function POST() {
  return NextResponse.json({ error: "not implemented" }, { status: 501 });
}
